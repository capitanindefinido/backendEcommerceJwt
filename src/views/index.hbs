<h1>Bienvenido</h1>
<h1>En tu perfil podrás revisar tu carrito...</h1>

<div class="row w-100">
    {{#each products}}        
        <div class="card w-25 p-0">
            <div class="card-header w-100 bg-dark text-white">
                <h3 class="text-center">{{this.title}}</h3>
            </div>
            <div class="card-header w-100 bg-dark text-white">
                <h3 class="text-center">{{this.description}}</h3>
            </div>
            <div class="card-footer w-100 bg-dark">
                🤍
                <!-- Verifica si _id es válido antes de usarlo en el enlace -->
                {{#if this._id}}
                    <a href='/product-detail/{{this._id}}' class="text-white">Detalle</a>
                {{/if}}

                <label>
                    👍
                </label>
            </div>
        </div>
    {{/each}}
</div>

<center>
    {{#if hasPrevPage}}
        <a href="/products?numPage={{prevPage}}"><button>Anterior</button></a>
    {{/if}}
    <label>{{page}}</label>
    {{#if hasNextPage}}
        <a href="/products?numPage={{nextPage}}"><button>Siguiente</button></a>
    {{/if}}
</center>
